import React from "react";
// will need to redirect to the home page. Perhps use Effect and set a timer to redirect to /dogs.

const NotFound = () => {
  return(
    <h1>Not Found</h1>
  )
}

export default NotFound;

